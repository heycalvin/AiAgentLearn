# 第1章 Agent基础知识

## 这章在说什么

这一章讲最核心的公式:

`Agent = LLM + 上下文 + 工具`

意思很简单，Agent 不是一个神秘黑盒，而是把模型、信息和动作拼在一起。

## 你可以这样理解

- `LLM` 负责想和生成。
- `上下文` 负责让模型知道“现在发生了什么”。
- `工具` 负责让它真的去查、算、写、调接口。

## 一个简单例子

你让 AI 帮你订机票。

它先理解你的需求，再看可选方案，最后调用工具去完成查询或预订。

## 记住一句话

Agent 强不强，不只看模型聪不聪明，还看它能不能把思考变成行动。
